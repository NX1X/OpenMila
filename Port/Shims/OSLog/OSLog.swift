// Copyright 2026 NX1X. Licensed under the Apache License, Version 2.0.
//
// `import OSLog` off macOS. The implementation lives in OpenMilaLogging so
// that `OSLog` and `os` expose one `Logger` type, not two ambiguous ones.
@_exported import OpenMilaLogging
